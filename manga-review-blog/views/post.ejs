<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= data.title_en %> - Review</title>
</head>
<body>
    <nav>
        <a href="/">← Back to Home</a>
    </nav>

    <main>
        <header>
            <h1><%= data.title_en %></h1>
            <h3><%= data.title_jp %></h3>
            <p><strong>Author:</strong> <%= data.author %></p>
            <p><strong>Genre:</strong> <%= data.genre %></p>
        </header>

        <section id="manga-meta">
            <img src="<%= data.cover_url %>" alt="Cover of <%= data.title_en %>" width="250">
            
            <div class="rating-badge">
                <strong>My Rating:</strong> <%= data.rating %>/10
            </div>
            
            <p>
                <a href="https://myanimelist.net/manga/<%= data.mal_id %>" target="_blank">
                    View on MyAnimeList
                </a>
            </p>
        </section>

        <hr>

        <article class="review-body">
            <p><em>Published on: <%= data.date_added.toDateString() %></em></p>
            <h3>My Thoughts</h3>
            <p><%= data.full_review %></p>
        </article>
    </main>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Manga Review Blog</title>
</head>
<body>

    <header>
        <h1>Manga Review Blog</h1>
        <p>A personal space where I track and review the manga I've read. This project uses the Jikan API and PostgreSQL.</p>
    </header>

    <section id="controls">
        <button>Order by Date</button>
        <button>Order by Rating</button>
        <button>Order by Title</button>
    </section>

    <hr>

    <main id="reviews-container">
        <% reviews.forEach(review => { %>
            <% const manga = mangas.find(m => m.id === review.manga_id); %>
            
            <article class="review-summary">
                <h2><%= manga.title_en %></h2>
                
                <img src="<%= manga.cover_url %>" alt="Cover of <%= manga.title_en %>" width="100">
                
                <p><strong>Rating:</strong> <%= review.rating %>/10</p>
                <p><strong>Added on:</strong> <%= review.date_added %></p>
                <p><strong>Summary:</strong> <%= review.summary %></p>
                
                <a href="/review/<%= review.id %>">View Full Review</a>
                <hr>
            </article>
        <% }); %>
    </main>

</body>
</html>